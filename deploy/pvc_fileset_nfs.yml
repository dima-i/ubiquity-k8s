kind: PersistentVolumeClaim
apiVersion: v1
metadata:
  name: "ubiquity-claim-fileset-nfs"
  annotations:
    volume.beta.kubernetes.io/storage-class: "spectrum-scale-fileset-nfs"
spec:
  accessModes:
    - ReadWriteMany
  resources:
    requests:
      storage: 1Gi
